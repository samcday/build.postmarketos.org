name: Sync Upstream And Rebase

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-upstream-and-rebase
  cancel-in-progress: false

jobs:
  sync-and-rebase:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout master
        uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "Sam Day"
          git config user.email "me@samcday.com"

      - name: Fetch upstream master
        run: |
          if ! git remote get-url upstream >/dev/null 2>&1; then
            git remote add upstream https://gitlab.postmarketos.org/postmarketOS/build.postmarketos.org.git
          fi
          git fetch upstream master

      - name: Update upstream mirror branch
        run: |
          git checkout -B upstream-master upstream/master
          git push --force-with-lease origin upstream-master

      - name: Rebase master on upstream mirror
        run: |
          git checkout -B master origin/master
          if ! git rebase origin/upstream-master; then
            git rebase --abort || true
            echo "Rebase failed. Resolve conflicts on master manually."
            exit 1
          fi
          git push --force-with-lease origin master
